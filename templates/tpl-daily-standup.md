<%*
// ═══════════════════════════════════════════════════════════════════════════
// 📊 DAILY STANDUP TEMPLATE  
// ═══════════════════════════════════════════════════════════════════════════
// Quick daily standup capture - focused on blockers, progress, and priorities
// Optimized for speed: minimal prompts, maximum value
// ═══════════════════════════════════════════════════════════════════════════

// Prompt for project/team context
const projectName = await tp.system.prompt("Project/Team:", "MVP");
const clientName = await tp.system.prompt("Client (leave blank for internal):", "");

// Date handling
const standupDate = tp.date.now("YYYY-MM-DD");
const dayOfWeek = moment().format("dddd");
const fullDate = moment().format("MMMM D, YYYY");
const nextStandup = moment().add(1, 'days').format("dddd, MMMM D");
const yearTag = moment().format("YYYY");

// Generate filename
const safeProject = projectName ? projectName.replace(/[^a-zA-Z0-9\s]/g, '').replace(/\s+/g, '-').toLowerCase() : "standup";
const filename = standupDate + "-" + safeProject + "-standup";
await tp.file.rename(filename);

// Build tags array
const clientTag = clientName ? clientName.toLowerCase().replace(/\s+/g, '-') : "internal";
const tagsArray = ["standup", "meeting", clientTag];
if (projectName) {
    tagsArray.push(safeProject);
}
tagsArray.push(yearTag);
const tagsYaml = tagsArray.map(t => "  - " + t).join("\n");

// Client display for header
const clientDisplay = clientName ? " | **Client:** " + clientName : "";
const clientYaml = clientName || "Internal";
const projectYaml = projectName || "";
-%>
---
tags:
<% tagsYaml %>
client: <% clientYaml %>
project: <% projectYaml %>
meeting_type: standup
meeting_date: <% standupDate %>
duration_min: 15
status: completed
created: <% tp.date.now("YYYY-MM-DD HH:mm") %>
---

# 📊 Daily Standup - <% projectName %>

> **Date:** <% dayOfWeek %>, <% fullDate %>  
> **Project:** <% projectName %><% clientDisplay %>

---

## 👥 Team Updates

### [[Person 1]]

| Yesterday | Today | Blockers |
|:----------|:------|:---------|
|  |  |  |

---

### [[Person 2]]

| Yesterday | Today | Blockers |
|:----------|:------|:---------|
|  |  |  |

---

### [[Person 3]]

| Yesterday | Today | Blockers |
|:----------|:------|:---------|
|  |  |  |

---

## 🚧 Blockers & Escalations

> [!warning] Issues requiring attention

| Blocker | Owner | Impact | Action Needed |
|:--------|:------|:-------|:--------------|
|  |  |  |  |

---

## 🎯 Sprint/Iteration Focus

**Current Sprint Goal:** 

**Days Remaining:** 

**On Track?** ⬜ Yes | ⬜ At Risk | ⬜ Behind

---

## 📝 Parking Lot

> Items raised that need discussion outside standup

- [ ] 
- [ ] 

---

## ✅ Quick Actions

| Action | Owner | Due |
|:-------|:------|:----|
|  |  |  |

---

## 📊 Key Metrics (Optional)

| Metric | Current | Target | Status |
|:-------|:--------|:-------|:------:|
| Stories Completed |  |  | 🟢/🟡/🔴 |
| Bugs Open |  |  |  |
| PRs in Review |  |  |  |

---

**Next Standup:** <% nextStandup %>

---

**Created:** <% tp.date.now("YYYY-MM-DD HH:mm") %>

[//begin]: # "Autogenerated link references for markdown compatibility"
[todo]: ../../todo.md "Todo"
[//end]: # "Autogenerated link references"
